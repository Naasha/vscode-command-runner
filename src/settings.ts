// À l'endroit approprié de votre code (par exemple, dans un fichier .ts dédié aux interfaces)
interface CommandRunnerSettings {
    customCommandsFile: string;
    // Ajoutez ici d'autres propriétés de configuration si nécessaire
}
